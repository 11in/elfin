/**
 * This collects JavaScript for our "legacy" build. This JS will be served to
 * older browsers.
 *
 * The `scripts/main` import should always be here, but you may want to add
 * things like polyfills, etc, to handle specific functionality in older
 * browsers.
 */

import './scripts/main';
